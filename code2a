# approach 2  migratory birds

from collections import Counter
def migratoryBirds(arr):
    x=(Counter(sorted(arr)))
   
    
    return max(x,key=x.get)

from collections import Counter
arr = [1, 2, 3, 4, 5, 1, 2, 3, 4, 1]
print(migratoryBirds(arr))

arr = [1,2,1,2,3,4,5]
print(migratoryBirds(arr))

arr = [1,2,3,4,5,4,3,2,1,3,4,4,3]
print(migratoryBirds(arr))
